// import { CameraPort } from '../Scenes/camera/camera-port';
// import { mock } from 'jest-mock-extended';
// import Gameplay from '../Scenes/game';
// import { ControlPort } from '../Scenes/controls/control-port';
// import  { Scene } from 'phaser';
// const Phaser = require("phaser");
describe('Camera', () => {
    it('should be tested', () => {
        
        // const cameraPort = mock<CameraPort>();
        // const controlPort = mock<ControlPort>();
        // const phaser = mock<Phaser>();
        // const scene = new Scene({ visible: true });
        // const game = new Gameplay({ cameraPort, controlPort, scene });

        // let games = new Phaser.Game({ type: Phaser.HEADLESS });

        // game.create();
        // game.update(0, 0);

        // expect(game).toBeDefined();
        // expect(cameraPort.setFollow).toHaveBeenCalled();
        // expect(cameraPort.update).toHaveBeenCalled();

    });
});


